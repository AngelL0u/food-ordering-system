package com.jumpstart.food_ordering_system.config;

import org.springframework.context.annotation.Configuration;

// Configuration classes are used to define beans and application settings.
// Spring reads these classes at startup to configure the application context.
@Configuration
public class ApplicationConfig {

}